{
  "type": "object",
  "properties": {
    "candidates": {
      "type": "array",
      "maxItems": 5,
      "items": {
        "type": "object",
        "properties": {
          "fingerprint": {"type": "string"},
          "title_fa": {"type": "string"},
          "problem_fa": {"type": "string"},
          "priority": {"enum": ["urgent", "high", "medium", "ux"]},
          "confidence": {"type": "number", "minimum": 0, "maximum": 1},
          "signal_fingerprints": {"type": "array", "items": {"type": "string"}},
          "reproducible_hint": {"type": "string"}
        },
        "required": ["fingerprint", "title_fa", "problem_fa", "priority", "confidence", "signal_fingerprints", "reproducible_hint"],
        "additionalProperties": false
      }
    }
  },
  "required": ["candidates"],
  "additionalProperties": false
}
